import React, {Component} from 'react';

export default class ConnectionFailed extends Component {
  render() {
    return (
      <div>This is a placeholder for when the connection fails.</div>
    );
  }
}
